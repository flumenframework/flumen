name: dependency
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish: none

environment:
  sdk: '>=2.9.0 <3.0.0'

dependencies:
  flumen_runtime:
    path: ../../..

dev_dependencies:
  pedantic: ^1.0.0
  test: ^1.0.0
