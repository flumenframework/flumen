name: flumen_test
version: 0.2.0
description: Utilities for writing automated tests for Flumen applications

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flumen: ^0.3.0
  matcher: '>=0.12.3 <0.14.0'
  test: ^1.3.0

# This is removed before being published.
# dependency_overrides:
#   flumen:
#     path: ../flumen
